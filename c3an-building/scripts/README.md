# Repo Scripts

Add new Python utility scripts in this top-level `scripts/` directory, next to the existing repo maintenance scripts.

- Python script path: `scripts/<name>.py`
- Example: `scripts/sync_registry.py`

These scripts are standalone developer utilities and are not bundled into the Vite frontend under `src/`.
